# 2026-07-24

## 企业微信官方适配器基础

- 恢复了 `pnpm-lock.yaml` 已声明依赖，并将跨项目测试从 `D:` 绝对路径改为相对兄弟目录。
- 新增 Rag 与网关兼容的 AES-256-GCM 运行配置密文格式，以及 `gateway.config.apply/applied` 协议。
- 新增密文运行配置存储、单调版本校验、同步游标存储和认证配置应用确认。
- 新增企业微信回调 SHA1 验签、AES-256-CBC 解密、精确 XML 标签读取和接收方校验。
- 新增官方 token 缓存、并发刷新、`sync_msg` 分页、文本/媒体元数据标准化和稳定事件 ID。
- 新增 `/webhooks/wecom/:callbackKey` GET/POST 路由、回调到现有 Outbox 的入站编排，以及锁定/白名单出站闸门。
- Rag 新增显式运行配置发布 API；只有网关确认同连接同版本后才标记连接为 active。
- 前端企业连接表新增“发布到网关”，只有已验证且已有同步账号的连接可操作。

## 验证

- `wehook` 原有基线：21 项测试通过。
- 企业微信模块和真实本机 WebSocket/HTTP 离线闭环已通过，所有平台 API 都由本地桩替代。
- 验证使用生成密钥、假企业、假客服账号和假客户 ID，没有连接企业微信公网，没有发送平台消息。

## 生产限制

- “客服1号”继续固定为生产保护，运行快照强制 `locked` 且 `aiEnabled=false`。
- 未配置公网 HTTPS、企业微信后台回调、真实测试白名单、媒体下载/上传和 Redis 前，不得上线 AI 自动发送。
- `README.md` 现有合并冲突标记未在本次任务中修改。
