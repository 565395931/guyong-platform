## 企业微信重新接入进展

- `wehook` 新增本地 `.env` 读取，启动器现在会自动加载同目录配置，避免重启后丢失 `WECOM_RUNTIME_CONFIG_KEY`。
- 已为 `Rag/rag-server/.env` 和 `wehook/.env` 写入同一条本地企业微信运行时共享密钥；密钥未回显到对话。
- `wehook` 网关、`Rag` 后端和平台相关测试已重新跑过，企业微信/云网关链路仍然通过。
- 本机 `wehook` 已恢复企业微信适配器，`/webhooks/wecom/<callbackKey>` 路由可用。
- 目前阻塞在真实企业微信后台登录态失效，且数据库里尚无企业微信连接记录；还需要 CorpID、微信客服 Secret、回调 Token、EncodingAESKey 和公网回调地址才能完成保存、验证与发布。

验证：

- `npm run check`（`E:\project\project\wehook`）
- `npm test`（`E:\project\project\wehook`）
- `npm run test:platform-connections`（`E:\project\project\Rag\rag-server`）
- `node --test src/modules/cloud-gateway/*.test.js`（`E:\project\project\Rag\rag-server`）
