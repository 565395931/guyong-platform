# 2026-09-24

## 多租户 SaaS 控制平面

- 在 `src/saas` 增加文件持久化控制平面、唯一运营管理员、租户管理员、设备凭证哈希、企业微信安装归属和追加式额度账本。
- 额度调用采用 `grant → reserve → settle/cancel`；所有金额均为整数 credits，预留、重复请求和返还具备幂等约束，当前不接支付。
- 新增 `cloud-console`：`admin` 用于租户/企微/额度运营，`account` 用于客户余额与设备，UI 设计和行为分别记录于 `DESIGN.md` 与 `UX-CONTRACT.md`。
- 生产机 `47.97.225.205` 已安装 Ubuntu Node.js 18，创建受限用户 `lonelywarrior`，服务部署到 `/opt/lonely-warrior/control-plane` 并由 `lonely-warrior-control-plane.service` 监听 `127.0.0.1:8790`。
- 唯一系统管理员用户名为 `admin`，初始密码按用户要求为 `admin123`，首登必须改成至少 12 个字符；无第二运营管理员创建入口。
- `admin/account/api` 的 Let's Encrypt HTTP-01 请求被外部 `Server: Beaver` 以 403 拦截，服务器本地 Nginx challenge 文件返回 200。结合域名未完成 ICP 备案，公网 HTTPS 暂未启用；不要为绕过此限制开放明文后台。
- 验证：`npm run check`、`npm test`（50/50）、Premium UI strict audit（0 findings）、生产服务器 `/healthz` 与本地 Host 路由通过。
