# 2026-07-25

## Windows 本机部署

- 用户确认现阶段不使用阿里云，先部署在当前 Windows 电脑，后续保留迁移能力。
- MySQL、Rag 后端和网关继续复用现有本机进程；新增项目内 Memurai 4.1.2（Redis API 7.2.5）运行时，只绑定 `127.0.0.1:6379`，启用 AOF 持久化。
- `platform-web` 完成生产构建并在当前 WLAN 地址 `192.168.1.4:3003` 提供服务；`/api` 与 `/socket.io` 继续代理到本机 `3001`。
- Rag 新增 `tools/local-deploy/start-local-platform.ps1` 和对应 dry-run 合同测试；脚本使用相对项目路径，按 Redis、网关、后端、前端顺序启动，已有端口会跳过。
- 企业微信网关仍只监听本机 `8787/8788`；公网只应暴露 `/webhooks/wecom/:callbackKey` 的 HTTPS 入口，不暴露 MySQL、Redis、管理前端或网关健康接口。

## 安全状态

- “客服1号”保持生产保护，未修改、未绑定真实回调、未发送消息。
- “AI测试客服”保持仅人工接待；AI 自动发送仍关闭。
- Redis 可用只代表队列基础设施恢复，不代表允许启用 AI 自动发送。
- 企业微信公网 HTTPS 回调、可信 IP、真实测试联系人白名单和媒体闭环仍待完成。

## 本机运行验收

- 本机生产入口为 `http://192.168.1.4:3003`；页面、静态资源、`/api` 代理和 Socket.IO 握手均已验证。
- MySQL `rag_customer_service` 可查询且当前有 40 张业务表；Redis 返回 `PONG`，AOF 已启用且最近写入状态正常。
- 网关测试 46 项、Rag 后端测试 81 项、统一前端测试 24 项全部通过；前端生产构建通过。
- 用户级计划任务 `RagLocalCustomerService` 已创建，登录后延迟 30 秒启动；2026-07-25 手动触发验收返回 `0`。
- 当前 Windows 公用网络已有 `E:\Node.js\node.exe` 的 TCP 入站放行规则，因此同一局域网电脑可访问前端；电脑必须保持开机并完成该 Windows 用户登录。

## 企业微信回调阻塞

- 企业微信拒绝 `wecom.thelonelybrave.cn`，提示域名备案主体与当前企业主体不一致或无关联关系；失败发生在回调 Token/AES 校验之前。
- 临时添加的 AAAA 解析已删除，未保存任何企业微信回调配置，也未修改现有应用 Secret。
- `fhl.mom` 对中国区访问返回 403，不能作为稳定回调入口。
- 待 `thelonelybrave.cn` 完成与“孤勇者（常州）新材料科技有限公司”一致或官方认可关联主体的 ICP 备案后，再配置 HTTPS 回调、可信 IP和仅限测试账号/白名单的联调。

### ICP 重新提交

- 备案订单 `2036806146428` 的阿里云初审已经通过；上次失败仅因为未在 24 小时内完成工信部短信核验。
- 2026-07-25 14:31 已点击“重新提交管局”，当前状态为“工信部短信核验：待验证”。
- 工信部核验短信发送至备案负责人手机 `180****7855`，需在 24 小时内由该手机点击短信链接完成验证；完成后阿里云状态同步通常需要 30～120 分钟。
- 短信核验完成后仍需等待管局审核，备案通过前企业微信不会接受该域名作为回调域名。
