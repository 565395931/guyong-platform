# 2026-09-23

- 清理 `README.md` 中遗留的 Git 合并冲突标记，保留 Cloud Channel Gateway 的实际项目说明。
- 在上级项目新增统一启动编排：本地 MySQL、Redis、网关、Rag 后端和统一工作台按顺序启动。
- 网关本身的协议、运行时行为、真实平台配置和凭据均未修改。
- 网关测试完整复跑通过：46/46；未连接真实平台或生产服务。
- 为 `api.lonely-warrior.online` 部署 Nginx + Let's Encrypt HTTPS，固定企业微信单一公网回调地址为 `/wecom/app/callback`，同时承载 GET URL 验证和 POST `kf_msg_or_event`。
- ECS 使用最小权限 `wecom-tunnel` 账号，只允许反向转发到 `127.0.0.1:18788`；本机通过 SOCKS5 `127.0.0.1:7890` 维护到本地网关 `127.0.0.1:8788` 的隧道。
- 已验证 ECS 可经隧道访问本地 `/healthz`，伪造回调查询能到达本地 WeCom adapter 并被拒绝为 `invalid callback`。
- 当前 `platform_connections` 为空；企业微信后台正式保存前，仍需在本地管理平台创建、验证并发布 WeCom 运行配置，再将公网固定路径映射到生成的内部 `callbackKey`。
